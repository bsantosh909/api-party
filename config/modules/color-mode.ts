import { ColorModeConfig } from '@nuxtjs/color-mode/types/color-mode'

const colorConfig: ColorModeConfig = {
  preference: "dark",
  classSuffix: "",
  storageKey: "color-mode"
}

export default colorConfig
